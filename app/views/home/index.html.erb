<h1>Welcome to the App!</h1>

<% if user_signed_in? %>
  <h2>Your Profile</h2>
  <p>Nickname: <%= current_user.nickname %></p>
  <p>Age: <%= current_user.profile.age %></p>
  <p>Gender: <%= current_user.profile.gender %></p>
  <p>Interests: <%= current_user.profile.interests %></p>
  <p>Recommended partner: <%= current_user.profile.recommendation.text %></p>
<% else %>
  <p>You need to sign in or sign up before continuing.</p>
  <%= link_to 'Sign in', new_user_session_path %>
  <%= link_to 'Sign up', new_user_registration_path %>
<% end %>
